import { ClearZeroPipe } from './clear-zero.pipe';

describe('ClearZeroPipe', () => {
  it('create an instance', () => {
    const pipe = new ClearZeroPipe();
    expect(pipe).toBeTruthy();
  });
});
